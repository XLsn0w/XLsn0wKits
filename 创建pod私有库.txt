
$ pod lib create PodLib

找到Classes中的"ReplaceMe.m"文件删除，然后将自己所需要的文件放到这里目录下

cd到Example路径下， 执行pod install 更新Example项目中的pod


$ cd PodLib

$ pod lib lint

$ pod spec lint

$ pod repo add PodLib https://gitee.com/xlsn0w/PodSpec.git

$ pod repo push PodLib PodLib.podspec --sources=https://gitee.com/xlsn0w/PodSpec.git

